# malice/diec plugin configuration.

name = 'diec'
description = 'Detect-It-Easy file type + packer/protector identification'
category = 'exe'
version = '0.1.0'

accepts = [ '*' ]

[system]
cpu = 0.5
ram = 256 # MB

[settings]
